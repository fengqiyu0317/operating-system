# 第1章第5节：为内核支持函数调用 - 动手操作指南

> 参考文档：https://rcore-os.cn/rCore-Tutorial-Book-v3/chapter1/5support-func-call.html

## 一、本节目标

本节的核心任务是**分配并使用启动栈**，使内核能够支持函数调用，并将控制权从汇编代码转交给 Rust 编写的内核入口函数。

---

## 二、前置知识检查

在开始动手之前，请确认你能回答以下问题（如果对于自己的答案足够自信的话则可以直接进入实践部分）：

1. 如何使得函数返回时能够跳转到调用该函数的下一条指令，即使该函数在代码中的多个位置被调用？
2. 对于一个函数而言，保证它调用某个子函数之前，以及该子函数返回到它之后（某些）通用寄存器的值保持不变有何意义？
3. 调用者函数和被调用者函数如何合作保证调用子函数前后寄存器内容保持不变？调用者保存和被调用者保存寄存器的保存与恢复各自由谁负责？它们暂时被保存在什么位置？它们于何时被保存和恢复（如函数的开场白/退场白）？
4. 在 RISC-V 架构上，调用者保存和被调用者保存寄存器如何划分的？
5. `sp` 和 `ra` 是调用者还是被调用者保存寄存器，为什么这样约定？
6. 如何使用寄存器传递函数调用的参数和返回值？如果寄存器数量不够用了，如何传递函数调用的参数？

---

## 三、动手操作步骤

### 步骤1：修改 `entry.asm` - 分配启动栈空间

**文件位置**：`os/src/entry.asm`

**操作内容**：

```asm
# os/src/entry.asm
    .section .text.entry
    .globl _start
_start:
    la sp, boot_stack_top
    call rust_main

    .section .bss.stack
    .globl boot_stack_lower_bound
boot_stack_lower_bound:
    .space 4096 * 16
    .globl boot_stack_top
boot_stack_top:
```

**网页中的解释**：

- 第 11 行在内核的内存布局中预留了一块大小为 `4096 * 16` 字节也就是 **64KiB** 的空间用作接下来要运行的程序的栈空间
- 在 RISC-V 架构上，栈是从**高地址向低地址**增长
- 最开始的时候栈为空，栈顶和栈底位于相同的位置，用更高地址的符号 `boot_stack_top` 来标识栈顶的位置
- 用更低地址的符号 `boot_stack_lower_bound` 来标识栈能够增长到的下限位置
- 第 8 行将这块空间放置在一个名为 `.bss.stack` 的段中

---

### 步骤2：修改链接脚本 `linker.ld`

**文件位置**：`os/src/linker.ld`

**操作内容**：

```ld
.bss : {
    *(.bss.stack)
    sbss = .;
    *(.bss .bss.*)
    *(.sbss .sbss.*)
}
ebss = .;
```

**网页中的解释**：

- `.bss.stack` 段最终会被汇集到 `.bss` 段中
- 全局符号 `sbss` 和 `ebss` 分别指向 `.bss` 段除 `.bss.stack` 以外的起始和终止地址
- 我们在使用这部分数据之前需要将它们初始化为零

---

### 步骤3：在 `main.rs` 中实现 `rust_main` 函数

**文件位置**：`os/src/main.rs`

**操作内容**（基础版本）：

```rust
// os/src/main.rs
#[no_mangle]
pub fn rust_main() -> ! {
    clear_bss();
    loop {}
}
```

**网页中的解释**：

- 需要通过宏将 `rust_main` 标记为 `#[no_mangle]` 以避免编译器对它的名字进行混淆
- 不然在链接的时候，`entry.asm` 将找不到 `main.rs` 提供的外部符号 `rust_main` 从而导致链接失败
- 在 `rust_main` 函数的开场白中，我们将第一次在栈上分配栈帧并保存函数调用上下文，它也是内核运行全程中最底层的栈帧

---

### 步骤4：实现 `clear_bss` 函数

**文件位置**：`os/src/main.rs`

**操作内容**：

```rust
// os/src/main.rs
fn clear_bss() {
    extern "C" {
        fn sbss();
        fn ebss();
    }
    (sbss as usize..ebss as usize).for_each(|a| {
        unsafe { (a as *mut u8).write_volatile(0) }
    });
}
```

**网页中的解释**：

- 在内核初始化中，需要先完成对 `.bss` 段的清零
- 这是内核很重要的一部分初始化工作，在使用任何被分配到 `.bss` 段的全局变量之前我们需要确保 `.bss` 段已被清零
- 全局符号 `sbss` 和 `ebss` 由链接脚本 `linker.ld` 给出，分别指出需要被清零的 `.bss` 段的起始和终止地址

---

### 步骤5：（可选）配置 `.cargo/config`

如果你需要反汇编查看函数调用过程，可以修改此配置：

**文件位置**：`.cargo/config`

**操作内容**：

```
// .cargo/config
[build]
target = "riscv64gc-unknown-none-elf"

[target.riscv64gc-unknown-none-elf]
rustflags = [
    "-Clink-args=-Tsrc/linker.ld", "-Cforce-frame-pointers=yes"
]
```

**网页中的解释**：

- 这可以设置我们的默认编译目标
- 同时调整编译选项，设置链接脚本以及强制打开 `fp` 选项
- 这样才会避免 `fp` 相关指令被编译器优化掉

---

## 四、关键概念（来自网页）

### 1. 函数调用跳转指令

| 指令 | 功能 |
|------|------|
| `jal rd, imm[20:1]` | `rd ← pc+4`，`pc ← pc+imm` |
| `jalr rd, (imm[11:0])rs` | `rd ← pc+4`，`pc ← rs+imm` |

- 这两条指令在设置 pc 寄存器完成跳转功能之前，还将当前跳转指令的下一条指令地址保存在 rd 寄存器中
- 在 RISC-V 架构中，通常使用 `ra` 寄存器（即 `x1` 寄存器）作为 rd 对应的具体寄存器
- 函数返回时使用伪指令 `ret`，它会被汇编器翻译为 `jalr x0, 0(x1)`

### 2. 函数调用上下文

- **被调用者保存(Callee-Saved) 寄存器**：被调用的函数可能会覆盖这些寄存器，需要被调用的函数来保存的寄存器
- **调用者保存(Caller-Saved) 寄存器**：被调用的函数可能会覆盖这些寄存器，需要发起调用的函数来保存的寄存器

### 3. 栈帧结构

网页中提到的编译器生成的开场和结尾汇编代码：

```asm
# 开场
# 为当前函数分配 64 字节的栈帧
addi        sp, sp, -64
# 将 ra 和 fp 压栈保存
sd  ra, 56(sp)
sd  s0, 48(sp)
# 更新 fp 为当前函数栈帧顶端地址
addi        s0, sp, 64

# 函数执行
# 中间如果再调用了其他函数会修改 ra

# 结尾
# 恢复 ra 和 fp
ld  ra, 56(sp)
ld  s0, 48(sp)
# 退栈
addi        sp, sp, 64
# 返回
ret
```

---

## 五、Rust Tips（来自网页）

### 外部符号引用

`extern "C"` 可以引用一个外部的 C 函数接口（这意味着调用它的时候要遵从目标平台的 C 语言调用规范）。但我们这里只是引用位置标志并将其转成 `usize` 获取它的地址。

### 迭代器与闭包

`for_each` 用到了 Rust 的迭代器与闭包的语法，它们在很多情况下能够提高开发效率。如读者感兴趣的话也可以将其改写为等价的 for 循环实现。

### Unsafe

将 `.bss` 段内的一个地址转化为一个裸指针 (Raw Pointer)，并将它指向的值修改为 0。这在 C 语言中是一种司空见惯的操作，但在 Rust 中我们需要将它包裹在 `unsafe` 块中。这是因为，Rust 认为对于裸指针的解引用 (Dereference) 是一种 unsafe 行为。

---

## 六、完成标志

完成本节后，你应该：

- [x] 理解了函数调用时栈的作用
- [x] 完成了 `entry.asm` 的修改（分配启动栈）
- [x] 完成了链接脚本的修改
- [x] 完成了 `main.rs` 中 `rust_main` 和 `clear_bss` 的实现
- [x] 控制权成功从汇编代码转交给了 Rust 代码

**下一节预告**：基于 RustSBI 提供的服务成功在屏幕上打印 `Hello, world!`